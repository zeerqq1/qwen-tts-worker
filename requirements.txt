# Worker dependencies.
#
# torch/torchaudio come from the base image. They are NOT listed here on
# purpose: if pip re-resolves torch it can install a CPU build over the CUDA
# one, and the worker then runs many times slower with no error to notice.
# The Dockerfile passes the cu128 index as a safety net and asserts CUDA
# survived the install.
runpod>=1.7.0
qwen-tts
transformers==4.57.3
accelerate
soundfile
numpy
huggingface_hub
