# Serverless worker dependencies. Same note about torch as requirements-pod.txt:
# it comes from the base image and must not be re-resolved by pip.
runpod>=1.7.0
qwen-tts
transformers==4.57.3
accelerate
soundfile
numpy
huggingface_hub
